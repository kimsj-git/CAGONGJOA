import { Grid } from "semantic-ui-react"
const BlockListItem = (props) => {
  return (
    <Grid.Row>
        <Grid.Column>
          닉네임(뱃지?)
        </Grid.Column>
      </Grid.Row>
  )
}

export default BlockListItem